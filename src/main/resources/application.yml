spring:
  application:
    name=jobboard:

  datasource:
    url: jdbc:h2:mem:jobboard
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true